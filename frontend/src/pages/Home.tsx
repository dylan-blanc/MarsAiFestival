import React from "react";
import Hero from "../features/hero/components/Hero";

const Home = (): React.JSX.Element => {
    return (
        <main>
            <Hero />
        </main>
    );
};

export default Home;
